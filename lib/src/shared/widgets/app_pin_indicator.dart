import 'package:flutter/material.dart';

class AppPinIndicator extends StatelessWidget {
  const AppPinIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
